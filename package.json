{
	"name": "prahlads-workshop",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "astro dev",
		"dev:local": "astro dev --host localhost --port 4321",
		"build": "npm run notion:pull:optional && npm run images:optimize && astro build",
		"preview": "astro preview",
		"prepare": "astro sync || echo ''",
		"check": "astro check",
		"check:watch": "astro check --watch",
		"images:optimize": "node scripts/optimize-uploads.mjs",
		"admin:proxy": "npx decap-server",
		"notion:pull": "node scripts/sync-notion.mjs",
		"notion:pull:optional": "node scripts/sync-notion.mjs --optional",
		"notion:pull:prune": "node scripts/sync-notion.mjs --prune",
		"notion:watch": "node scripts/watch-notion.mjs"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.9",
		"@types/node": "^25.9.1",
		"astro": "^6.3.7",
		"sharp": "^0.34.5",
		"typescript": "^6.0.2",
		"vite": "^7.3.3"
	},
	"dependencies": {
		"@netlify/blobs": "^10.7.9"
	}
}
